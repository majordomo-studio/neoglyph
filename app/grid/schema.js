const schema = {
  // order: ['size', 'color', 'price', 'rating', 'stock'],
  filters: ['category', 'size'],
  badgeColumns: ['price', 'shipping_cost'],
  centerAlignedColumns: ['popularity'],
};

export default schema;
